[package]
name = "appcast-plugin"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Authoring kit for appcast transporter plugins (C ABI v1)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
